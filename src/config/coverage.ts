/**
 * Пороги покрытия. Храповик: значение двигается только вниз.
 *
 * Отправная точка 25.08 до шага 06.1: без группы сравнения 221 из 372 — 59.4%
 * по числу, 63.3 млрд по капитализации. После карты и исключений ожидается
 * ~38 из ~332 — 11.4% по числу и около 2% по капитализации.
 */
export const COVERAGE = {
  /**
   * Доля участников альфа-входа без comparisonGroup.
   * История: 59.4% до карты секторов → 10.12% после (прогон 26.08, база 336).
   * Только вниз: поднять порог значит объявить регресс нормой.
   */
  maxSectorGapPct: 11,
  /** Факт 26.08 — 1.8%: крупные пробелы закрыты все. Три процента здесь запас. */
  maxSectorGapMcapPct: 3,
  /**
   * Гейта на выручке нет: сетям нужны свои метрики, и комиссии майнеров доходом
   * держателя не являются. Доля измеряется и печатается, но ничего не блокирует.
   */
  revenueGate: false,
} as const;